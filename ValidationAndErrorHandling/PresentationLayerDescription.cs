using Core;

namespace Caching
{
    public class PresentationLayerDescription : ILayerDescription
    {
        public void LayerDescription()
        {
            //Presentation Layer(UI):
            //Controllers: For ASP.NET Core, this would be MVC controllers or API controllers that handle HTTP requests.
            //Views/Pages: Razor pages, MVC views, or any other UI components.
            //ViewModels: Models designed specifically to be used by the views.
            throw new NotImplementedException();
        }
    }
}
